Para compilar la aplicacion:
`cmake -B build && cmake --build build`

El binario sin instalar se encuentra en: **build/src/wxclip**

Para instalar el binario en: /usr/local/bin/
`cmake --install build`